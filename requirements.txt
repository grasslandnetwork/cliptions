# Core dependencies (required for basic functionality)
numpy>=1.24.0
torch>=2.0.0
transformers>=4.30.0  # For ClipEmbedder (HuggingFace implementation)
Pillow>=9.0.0
clip>=1.0  # Original OpenAI CLIP (imported in calculate_scores_payout.py)

# Development dependencies (uncomment to install for development work)
# jupyter>=1.0.0
# ipywidgets>=8.0.0
# matplotlib>=3.10.0
# notebook>=7.0.0
# open_clip_torch>=2.20.0  # Alternative CLIP implementation used in testing

# Testing dependencies (required for running tests)
pytest>=7.0.0

# Optional dependencies for specific features
# requests>=2.25.0  # For additional web functionality 